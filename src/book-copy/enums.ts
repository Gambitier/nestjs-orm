export enum BookStatus {
  AVAILABLE,
  RESERVED,
  LOANED,
  LOST,
}

export enum ReservationStatus {
  WAITING,
  PENDING,
  CANCELED,
  NONE,
}

export enum AccountStatus {
  ACTIVE,
  CLOSED,
  CANCELED,
  BLACKLISTED,
  NONE,
}
